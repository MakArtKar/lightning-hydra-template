defaults:
  - /transforms/image@transform.transforms.to_tensor: to_tensor
  - /transforms/image@transform.transforms.normalize: normalize

_target_: src.data.hf_datamodule.HFDataModule
hf_dict_dataset:
  _target_: datasets.load_dataset
  _args_:
    - ylecun/mnist

val_ratio: 0.1

transform:
  _target_: src.transforms.base.TransformCompose

dataloader_kwargs:
  batch_size: 128
  num_workers: 8
  pin_memory: True
