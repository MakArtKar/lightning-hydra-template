_target_: ml_core.data.base_datamodule.BaseDataModule
hf_dict_dataset:
  _target_: datasets.load_dataset
  path: ${params.data.dataset.path}
  name: ${oc.select:params.data.dataset.name,None}
val_ratio: 0.1

batch_size: 128
num_workers: 8
persistent_workers: True
pin_memory: True
